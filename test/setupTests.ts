// Global test setup to extend matchers
// If using Jest:
//   Add to jest.config.js: setupFilesAfterEnv: ['<rootDir>/test/setupTests.ts']
// If using Vitest:
//   In vitest.config.ts: test: { setupFiles: ['test/setupTests.ts'] }
import "@testing-library/jest-dom";